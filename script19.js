function addition(){
    fNum = +firstNum.value;
    sNum = +secondNum.value;
    result.value = fNum + sNum;
}
function multiplication(){
    fNum = +firstNum.value;
    sNum = +secondNum.value;
    result.value = fNum * sNum;
}

function subtraction(){
    fNum = +firstNum.value;
    sNum = +secondNum.value;
    result.value = fNum - sNum;
}

function division(){
    fNum = +firstNum.value;
    sNum = +secondNum.value;
    result.value = fNum / sNum;
}